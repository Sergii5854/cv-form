# CF Form 
## Description: 
```
Test React with efector
```
## Task 
```
1 submit button is disabled if requirements (validation) were not met
2 validation errors must be displayed below inputs (including validation for technologies sub-fields) ONLY AFTER user touched the field
3 validation errors must be displayed after submission with invalid fields
4 reset button must reset the form
5 loader is a must
6 show TY page on success
7 show API error message (somehow) on fail
8 you may use mocked api function to resolve or reject randomly after 1 second
```